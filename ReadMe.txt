How to use:
*Note that a GPS system must be set up

A computer must be running the host program, then on each turtle the client program must be run to connect to the host. A second computer must be used to send a message on
channel 4 to intate the minning sequance.